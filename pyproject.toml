[tool.poetry]
name = "swch_com"
version = "0.1.0"
description = ""

[tool.poetry.dependencies]
twisted = "*"
kademlia = "*"
miniupnpc = "*"
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
